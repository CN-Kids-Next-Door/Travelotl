/**
 * mock file for jest when testing React components
 */

// jest is looking for a test in this file. So this code fulfills its requirement and gets rid of the error
test('truthy', () => {
    expect(true).toBe(true);
});

module.exports = 'test-file-stub';